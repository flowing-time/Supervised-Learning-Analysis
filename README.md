# Instructions

1. Install the python environment with this command:

conda env create -f environment.yml

2. Execute the following command to activate the python environment:

conda activate CS7641

3. Execute the first command to get all dataset 1 related figures:

python run_data1.py

4. Execute the second command to get all dataset 2 related figures:

python run_data2.py
